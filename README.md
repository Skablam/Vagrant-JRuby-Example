Vagrant-JRuby-Example
=====================

A small repo to install JRuby in vagrant and run a test script.

Run these commands:

<pre>
vagrant up
vagrant ssh
cd /vagrant
jruby test_jruby.rb
</pre>

It should then print "Hello" in the terminal.
